# Python
__pycache__/
*.pyc
*.pyo

# Virtuelle Umgebungen
venv/
.venv/
build_venv/

# PyInstaller-Build-Artefakte
build/
dist/
*.spec

# Von PyInstaller/FFmpeg zur Laufzeit benoetigt, aber nicht ins Repo
# (Lizenzgroesse + wird von der GitHub-Action automatisch geladen)
bin/ffmpeg.exe
bin/ffmpeg

# Wird bei JEDEM CI-Build frisch mit dem echten Commit-Hash erzeugt
# (siehe .github/workflows/build.yml) - bewusst NICHT eingecheckt, damit
# re-sync-to-repro.sh (git add -A) niemals versehentlich einen von einem
# lokalen Testbuild stammenden, falschen Hash committet. Fehlt die Datei
# (z. B. bei python main.py aus dem Quellcode heraus), greift der
# "dev"-Fallback in gui_main.py.
build_info.py

# Editor-/OS-Muell
.vscode/
.idea/
*.bak
.DS_Store
Thumbs.db
